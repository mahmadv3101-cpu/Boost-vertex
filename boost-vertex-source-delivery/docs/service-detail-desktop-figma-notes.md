# Service Detail Desktop Figma Inspection Notes

## Latest Screenshot Geometry Confirmation

The latest wide workflow reference measures 1777×552 px and confirms that the section is one integrated panel: the upper media area is roughly 405 px high, while the attached bottom step rail is about 147 px high. The copy begins about 52 px from the left. The artwork begins near the midpoint and must retain the lime orbital line, five grid nodes, and technical status label without being obscured by an opaque overlay.

The companion 646 px reference confirms the intended adjacent sequence: a modest, compact six-card 3×2 capability grid directly above the integrated workflow panel. The capability cards require clear one-pixel boundaries, small numeric labels, and consistent card heights.

The latest outcomes reference measures 1828×304 px. Each proof panel is inset 15 px from its corresponding edge, with a 24 px central gap. The two cards are roughly 208 px tall, use a 1 px charcoal border, and keep all content left-aligned at about 36 px from the card edge: client label at the top, metric at the visual center, and one single-line caption low in the card.

The supplied single-card reference is 928×286 px. Its 1 px border sits roughly 25 px from the crop edge. The client label begins about 54 px from the left and 60 px from the top. The lime metric has a distinctly wide, display-style Chivo construction, with visibly looser word spacing than the current rendered result; the caption remains a single subdued line approximately 35 px below the metric.

The latest two-card proof reference is 1827×385 px. In the left card, the client label begins roughly 89 px from the frame’s left and 107 px from the top. The metric baseline begins roughly 88 px from the card edge and uses large lime display type. The first two crops confirm the left card copy is `Avg. +49% ROAS` with the dynamic description: `A backend-managed proof point showing how paid social & meta ads created a measurable commercial outcome.`

The final two crops confirm the second card repeats the same label, metric, and caption geometry. Its shown fallback text is `[REAL METRIC]`, with `A second approved case-study outcome can be connected here once the backend content source is available.` The requested implementation must replace this fallback with service-specific dynamic proof data while retaining the reference’s smaller, approximately 385 px-tall two-card band.

The current default-type reference is 1833×601 px. The proof panels are visually larger than the shallow band but the metric type itself is materially smaller than the 68 px implementation: it reads as approximately 50 px at the reference’s native 1280px component scale. The first two crops confirm the client label and caption retain their current compact hierarchy; only the lime metric needs reducing.

The final two crops verify that a multi-line service-specific metric uses the same reduced scale as the left one-line metric. It preserves a confident two-line treatment within the card without exceeding the right boundary. The calibrated metric must therefore use 50 px desktop type with the existing tight line-height, while retaining dynamic wrapping for longer metric text.

The latest compact proof-card screenshot is 1882×518 px. The left metric reads as a noticeably compact single line with substantial unused card width and should be approximately 40 px at the 1280px component scale. The client label and caption remain unchanged, confirming the user’s requested adjustment is limited to the lime metric typography and card-safe metric wording.

The final two crops confirm that the right card’s `HIGHER-VALUE CONVERSIONS` can remain intentionally two lines at the compact 40 px scale. It has generous right-side negative space, no clipping, and the caption remains clear below the second line. The dynamic Paid Social copy may retain this two-line metric after reducing its display size to the same 40 px scale.

## Overview

The supplied frame is a long-form reusable desktop service template presented beside a corresponding mobile version. The desktop template uses the existing obsidian, olive, and electric-lime Boost Vertex system. Its hero explicitly exposes reusable content placeholders: a service category label, an outcome-focused service headline, supporting copy, a primary strategy CTA, and a service-specific abstract technical artwork.

The overview identifies the desktop sequence as: hero with Fast Track rail; three-card **Why [Service] Matters** rationale section; pain-point diagnostic section; a horizontal **How we approach [Service]** process; deliverable and workflow sections; service proof/standards; a lime conversion panel; and a compact footer. Focused inspection is required to record exact copy, component geometry, and every dynamic placeholder.

## Focused Hero and Upper Content

The desktop reference is 1280 px wide. It begins with the shared 80 px dark header. The service hero is a roughly 300 px-tall olive-and-obsidian field with the existing curved green background geometry. It uses the eyebrow **[SERVICE CATEGORY]**, an oversized four-line uppercase Chivo headline **[SERVICE OUTCOME-FOCUSED HEADLINE]**, a 2–3 line supporting description, a lime primary CTA **Start Your Free Strategy Plan →**, and a dark outline secondary CTA **Book a Strategy Call →**.

The hero's right half carries a landscape, dark service-specific dashboard/technical media panel. A slim vertical Fast Track rail is positioned at the far right of the hero. A short trusted-brand/logo strip follows the hero.

**Why [Service] Matters** is centered on a near-black band and presents three equal dark cards with lime micro-icons and reusable titles: **Velocity & Scale**, **Robust Stability**, and **Data-Driven Insights**. The succeeding medium-dark diagnostic block has the headline **What’s holding your [service] performance back?**, a concise subhead, and four numbered two-column compact problem cards (`01`–`04`) using **[Problem Title]** and supporting placeholder copy.

**How we approach [Service]** is an olive-dark process band. It uses a thin horizontal timeline with one lime active dot and seven compact reusable stages: **Audit**, **Strategy**, **Build**, **Launch**, **Test**, **Optimize**, and **Scale**. The following section starts **Everything you need from [Service]** and contains a three-card capability grid.

## Focused Middle Content

The capability section uses six concise equal cards in a two-row, three-column grid. Each has a lime accent line, **[CAPABILITY NAME]**, and short explanatory placeholder text. Directly below, **Visualizing the Workflow** is a wide two-column image-led dark panel with an olive/green technical working image on the right. Three compact cards overlap its lower edge and use the reusable headings **Setup**, **Management**, and **Scaling**.

**Built for measurable outcomes** follows on a dark field. It pairs an olive technical panel and a compact black results card. Both expose interchangeable case-study placeholders: **[REAL CLIENT]**, **[REAL METRIC]**, and a short outcome description. The palette remains obsidian with narrow electric-lime rules, white Chivo display headings, and compact muted Inter body copy.

## Focused Lower Content

The lower half contains two equal black outcome cards beneath **Built for measurable outcomes**, each repeating the reusable `[REAL CLIENT]` and `[REAL METRIC]` pattern. **The Boost Vertex Standard** follows on a muted olive panel with five icon-and-copy assurances: **Strategy before spend**, **Outcomes over activity**, **Transparent reporting**, **Continuous optimization**, and a final iteration/partnership assurance.

The template includes a centered **Frequently Asked Questions** heading with five slim single-line accordion rows. The final pre-footer conversion band is full-width electric lime and reads **Ready to grow with [SERVICE]?**, with a dark primary CTA and a pale/outlined **Book a Free Strategy Call** secondary action. It is followed by the compact shared footer.

## Refinement Target

The approved Service Detail Figma composition has larger desktop type, cards, and vertical section rhythm than the first implementation. The hero must reuse the established Home curve geometry rather than a custom olive ellipse. Its right-side media should read as a dense dark operational dashboard with layered charts rather than a minimal metric card. The workflow must read as an image-led operator/technical composition, while measurable outcomes should retain the Figma’s two equal proof cards with stronger metric hierarchy and breathing room.

At high zoom, the source dashboard is a dense black monitoring panel: it has a pale compact top tool row, multiple small dashboard tiles, a line graph in the upper-left and a histogram/data block in the lower-left. It should occupy roughly 38% of the hero and be framed by the dark Home-like curve with soft lime ambient light. The target has materially larger headline, hero controls, cards, and section gaps than the first implementation.

## Supplied Outcomes Reference

The two measurable-outcome cards are equal-width, shallow horizontal panels. They use a flat near-black surface with a thin charcoal keyline and no decorative gradient. The contents begin at roughly 42px from the left and vertically center in the panel: a small lime layered-hex icon plus `[REAL CLIENT]` label, one dominant lime metric line, then a subdued single-line explanatory sentence. The left metric is `Avg. +49% ROAS`; the second intentionally remains `[REAL METRIC]`.

The right card repeats the same alignment and generous dark negative space. The metric is intentionally prominent but the panels do not include any secondary title under the number; the explanatory sentence is the only caption.

## Supplied Workflow Reference

The workflow section is a single wide near-black media panel. Its left third is a clean dark negative-space area with the lime uppercase eyebrow, large white `Visualizing the Workflow` heading, and two-line body text; it is not a small floating card. The right side is a dark blue-green technical field with a rectangular grid, slim lime orbital line, and isolated glowing lime nodes. The three workflow step cards are attached flush along the bottom edge of the media panel, with thin borders and equal widths.
