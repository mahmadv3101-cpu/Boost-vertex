# Services Overview Mobile Figma Inspection Notes

## Verified frame characteristics

The supplied Figma target is the **Mobile** Services Overview frame at node `6-2`. It uses the existing Boost Vertex obsidian canvas, electric-lime accent, Chivo display typography, a compact single-column composition, and a fixed mobile action rail.

## Verified visible sequence

At the currently inspected canvas scale, the mobile composition is a vertical flow: compact header and hero, connected-system visual, phone-orbit approach artwork with the three numbered steps, a centered **Our Core Capabilities** heading, and the capability cards in a one-card-per-row stack. The desktop art and cards are shown adjacent in Figma only for reference; the Mobile frame itself uses the single-column stack.

## Initial mobile-specific cues

The capability cards retain their dark panel treatment, subtle gray borders, lime ordering labels/metrics, compact content density, and a small right-facing action affordance. The approach content is vertically stacked with the paired-phone illustration above the approach copy and uses the same white/lime contrast system as the approved desktop route. Remaining top and lower-frame measurements will be captured before implementation.

## Top-of-frame details

The mobile frame is visibly **390px wide**. It begins with a compact obsidian header: lime Boost Vertex wordmark on the left and a small lime hamburger control on the right. The hero is center aligned and omits the desktop system-board panel. Its sequence is eyebrow, the two-line `Every channel. One growth system.` display heading, centered support copy, a full-width lime growth-plan button, and a full-width dark outline strategy-call button.

Immediately beneath the hero CTAs, the paired-phone/lime-orbit visual is centered at a compact mobile scale. It leads directly into the approach eyebrow, display heading, explanatory paragraph, three ordered steps, and `See How We Work` text action. The approach remains a single continuous dark area before the centered Core Capabilities heading.

## Approach and capability treatment

The mobile approach copy is left aligned beneath the centered phone visual. The numbered entries are compact, two-column rows with a lime two-digit number at left and the title/body at right. The capability section heading is center aligned, followed by full-width cards with small inter-card gaps. Each card keeps the desktop information architecture but uses mobile-scale type, a top order label/icon line, and a bottom metric/action row.

## Lower-frame details

Cards `04` through `06` continue the same narrow full-width stack. The lime conversion panel follows directly after the final card and becomes a centered, vertically arranged call-to-action block. Its dark primary button is full width inside the panel; the secondary action follows beneath it. The desktop footer is replaced by a compact mobile footer below this panel, while the shared mobile Fast Track rail remains fixed to the viewport rather than flowing within the document.

## Exact lower copy and footer structure

The mobile conversion block keeps `Not sure which service you need?` and uses `Get My Free Growth Plan` as its black primary action followed by an outlined `Book a Free Strategy Call` action. The compact footer presents the lime Boost Vertex wordmark and short positioning copy, then three small groups: **Services** (`SEO Strategy`, `PPC Management`, `Content Marketing`, `Web Development`), **Company** (`About Us`, `Careers`, `Case Studies`, `Contact`), and **Connect** (email, phone, LinkedIn, Twitter). It ends with copyright and `Privacy Policy` / `Terms of Service` links.

## Typography calibration note

The Figma desktop and mobile frames use a genuine Chivo family treatment across the Services route. Their display lines retain Chivo’s distinctive italic face but use a more controlled semi-bold/bold density than the current extra-bold browser rendering. Interface copy, labels, and body text also use Chivo with compact tracking rather than the heavier inherited weight treatment.
