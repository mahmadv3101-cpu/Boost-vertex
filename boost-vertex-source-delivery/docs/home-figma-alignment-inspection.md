# Home Figma Alignment Inspection

The supplied Home Figma frame was opened on 21 August 2026. The Figma document title loaded, but the canvas remained in its loading state in the sandbox viewer, so no additional reliable canvas measurements could be extracted from that session.

The implementation will therefore use the user’s explicit direction: keep the hero composition unchanged and center the relevant non-hero Home section heading and description groups.
