# Service Detail Mobile Figma Inspection Notes

## Initial Canvas Observation

The supplied Figma document presents a long, single-column mobile Service Detail frame alongside a desktop frame. The mobile design retains the established obsidian, olive, and electric-lime visual system. At the initial full-canvas view, the mobile frame contains the complete service-detail flow from compact hero through the lime conversion band and dark footer. Detailed section measurements and copy treatment will be added during focused canvas inspection.

## Focused Middle-Section Observation

At 50% canvas zoom, the mobile frame confirms a strict single-column layout. The **Everything you need from [SERVICE]** heading is centered on a dark field with a short centered paragraph beneath it. It is followed by six full-width stacked capability cards: each card uses a dark charcoal fill, fine border, compact index, placeholder capability name, and short body copy. The original desktop grid must collapse into these consistently spaced vertical cards on mobile. The workflow and proof modules continue below this stack.

The mobile frame is the selected long narrow frame in the Figma canvas. It retains a roughly 390px design width and shows the same section sequence as desktop, but converts all content modules into a single-column vertical rhythm. The mobile inspection will use this selected frame as the source for upper and lower module details.

## Lower-Middle Module Observation

The capability cards on mobile are spaced as individual rounded dark panels rather than a continuous desktop grid. They are roughly 80–95px tall at the 390px frame width, with 8–10px vertical gaps, left-aligned copy, and a compact numeric label. The visual workflow begins immediately after the six cards and changes into a narrow image-led module. The lower proof, Standard, FAQ, conversion, and footer modules remain a vertical continuation of the same mobile frame.

## Workflow Observation

On mobile, the workflow is not a large desktop-style technical canvas. It becomes a tall, narrow image panel with the supplied dark dashboard/portrait art behind a compact bordered copy overlay near the lower left. The embedded heading wraps to two lines, and the supporting copy remains compact. The three workflow stages follow in the mobile flow rather than functioning as a wide horizontal rail.

## Workflow-to-Outcomes Transition

The mobile workflow panel closes directly into a centered **Built for measurable outcomes** heading on the obsidian field. This confirms that the outcome proof cards are a sequential mobile stack rather than desktop-style equal-width columns. The mobile layout preserves dark, bordered proof modules and the high-contrast lime metric treatment, but must give each proof point its own full-width vertical space.

## Proof-Card Observation

Each mobile proof point is a full-width dark card with a clear charcoal border and generous vertical breathing room. The label remains at the upper-left, while the metric appears centered lower in the card with the supporting description beneath it. The mobile Figma source uses placeholder proof data; the implementation must retain the existing dynamic per-service proof content in this same one-card-per-row composition.

The lower-frame inspection confirms two proof cards appear consecutively in the mobile outcomes band. They use a compact, card-contained metric rather than the large desktop proof type. The outcome heading remains centered immediately above the stack, and the following lower modules continue after both proof cards.

The selected-frame review also confirms the mobile art and card system is intentionally narrower and more vertically paced than the desktop template; no desktop full-width media or multi-column rail should survive below the mobile breakpoint.
