# Blog Detail Figma Inspection Notes

## Initial canvas review

The supplied Figma file provides coordinated desktop and mobile Blog Detail frames. The desktop frame is a tall, narrow editorial page that begins with the existing dark Boost Vertex header, then a category eyebrow, a large multi-line article title, and article metadata. The main reading layout pairs a compact contents rail with a single primary article column. The template includes several dark callout panels, an author block, accordion-style FAQs, a lead-generation form, related reading cards, a contained lime conversion banner, and the shared dark footer. The mobile frame follows the same content sequence in one column.

## Verified body structure

The desktop article column is approximately half the page width and uses white **Chivo** headings with fine grey Inter body copy. The current Figma article is titled `SaaS Application Development: From Idea to Launch and Scaling` and is composed as sequential `Phase` sections. It includes a bright lime rule beneath major section headers, compact checklist bullets, a dark two-stat evidence card (`6–10` weeks for MVP and `1` core workflow), and a full-width charcoal **Key Takeaways** panel with three lime bullet markers. The mobile frame preserves these elements in a narrow single column.

The desktop contents rail mirrors the article heading sequence and is a dark sticky panel with a thin lime active indicator. The Figma callout cards use low-radius dark surfaces rather than elevated rounded cards, with lime reserved for rules, markers, figures, and active states.
