# Case Study Detail Desktop Figma Inspection Notes

## Initial Full-Frame Observation

The supplied Figma file contains paired mobile and desktop Case Study Detail template frames. The desktop frame is a long single-page case-study narrative in the existing Boost Vertex dark, olive, and electric-lime system. The visible section sequence is: shared navigation; large outcome-led hero with an italicized lime word in the heading; compact top-line performance metrics; project summary and marketing-system panels; the challenge; the process; execution detail; an impact band; results/proof; related case studies; a lime conversion section; and the shared footer. The template is suitable for a typed dynamic route.

## Focused Middle-Section Observation

The desktop content column is intentionally narrow and left-aligned within a wide dark page field. The challenge presents three short charcoal bordered cards for **Stagnant Lead Quality**, **Escalating Costs**, and **Fragmented Data**. It is followed by a vertical four-step **The Process** timeline—Audit, Strategy, Execute, and Optimize—with lime-ringed numeric markers connected by a fine line. **How We Executed It** is a compact two-column detail list with labeled rows for Meta Ads Restructuring, Custom Landing Pages, and Server-Side Tracking. Typography is dense, small, and editorial rather than dashboard-like.

## Impact-Band Observation

After the execution-detail list, the design introduces **The Impact** inside a charcoal, softly textured full-column panel. It uses oversized lime and white performance figures in one horizontal row, including a +47% lift, 3.2x return, −31% efficiency measure, and +68% rate measure. This band acts as the visual turning point before the longer results, related-work, and conversion content.

The impact labels are **Qualified Lead Volume** (+47%), **Return on Ad Spend** (3.2x), **Cost per Acquisition** (−31%), and **Conversion Rate Growth** (+68%). These exact labels should become reusable typed case-study outcome data.

## Post-Impact Quote Observation

Immediately after the impact band, the desktop design uses a dark bordered testimonial quotation panel with a small lime quotation mark. Its copy positions Boost Vertex as having re-engineered the client’s full-funnel digital-acquisition engine rather than merely running ads. This should be treated as an attributed case-study quote field, not fabricated user-review content; the template will include only the project-supplied proof quote.

## User-Supplied Screenshot Correction Specification

The screenshot set supersedes the earlier wide-page interpretation. The correct desktop template is a narrow centered editorial dossier. The hero contains only a centered category label, a three-line centered headline—**Turning paid social into a predictable growth channel.**—a centered one-sentence summary, and three compact impact metrics. It does not include a dashboard panel. A thin divider leads to a centered trust-logo row.

The main narrative has a left contents rail and one narrow content column. The project overview is a single compact four-field strip. **The Challenge: A stalled GTM engine.** uses three stacked horizontal cards with an icon and text, not a three-column grid. The four-step process is a small vertical line with lime-ring markers; execution is a compressed two-column row list. The impact panel contains exactly +47% Qualified Leads, 3.2x ROAS, −31% Cost Per Lead, and +68% Conversion Rate Growth.

The supplied third screenshot contains project-provided proof content: the dark bordered quote card has the exact visible quote beginning *“Boost Vertex didn’t just run our ads…”* and the visible attribution **Marcus Vance, CMO at Nexus Pay**. The final section is **Further Reading**: three short visual reading cards and a small right-aligned View All link. The lime conversion CTA is a contained rounded banner with left-aligned copy and a black **Book a Strategy Call** button; it is not full-width. The shared footer follows this banner.

## Further Reading Visuals

The Figma canvas confirms three distinct visual cards: a dark technical network/connected-points image, a dark server-rack infrastructure image, and a high-contrast abstract black geometric image with a diagonal white beam. These must replace the prior CSS-only placeholder artwork. The desktop Fast Track treatment is the established narrow right-side vertical home-page tray with four icon cells beneath a small lime Fast Track label.

At full canvas scale, the template’s narrative headings and body copy are materially larger than the initial narrow implementation: process headings sit around the visual weight of a 32px display title, while step labels and body copy use readable editorial spacing. The revised page should therefore increase the desktop content column, heading scale, card padding, and vertical section rhythm together rather than only enlarging isolated text.

The full-scale Figma canvas also confirms that the process timeline has generous vertical gaps and the execution list uses broad two-column spacing with readable multi-line descriptions. This supports scaling the entire detail system—rather than simply stretching the hero and closing modules.

The full-scale execution block uses a larger section title, a visible lime left rule, wide row spacing, and a deliberately broad second text column. These proportions should be applied along with the enlarged case-detail content system.

The full Figma Further Reading row confirms that the images occupy the upper portion of three 268px-wide rounded cards. Their title/category copy is separate beneath each image. The first visual includes a small terminal-interface frame over a white connected-node network; the second is a high-contrast black server-rack photograph; and the third is a matte black low-poly geometric form intersected by a bright diagonal beam. The crop should preserve only the top image region, excluding the visible category and headline text below.
