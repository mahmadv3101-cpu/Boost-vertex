# Case Study Detail Mobile Figma Inspection Notes

## Initial canvas review

The supplied Figma file contains a narrow mobile frame alongside the approved desktop design. The mobile composition retains the Case Study Detail content sequence but uses a single-column, vertically paced layout. The frame starts with a compact dark hero and continues through overview, challenge, process, execution, impact, project insight, further reading, a lime conversion banner, and footer. Further zoomed inspection is required to record the exact mobile spacing, typography, panel geometry, and mobile-only behavior before implementation.

## Verified mid-page specification

At the mobile frame's 50% canvas view, the middle of the page uses a **narrow, left-aligned single column**. The process appears as four vertically linked, numbered lime-outline steps (`Audit`, `Strategy`, `Execute`, `Optimize`) rather than the desktop-side-by-side presentation. `How We Executed It` follows with three stacked dark execution cards: `Meta Ads Restructuring`, `Custom Landing Pages`, and `Server-Side Tracking`.

`The Impact` is a vertical run of **four individual charcoal metric cards** with lime headline figures and a smaller centered caption: `+47% Qualified Leads`, `3.2x Return on Ad Spend`, `-31% Cost per Lead`, and `+68% Sales Velocity`. These cards are full-width within the narrow content column, separated by compact dark gutters.

## Latest verified proof details

The mobile Figma frame confirms that **all four** Impact metric figures use the electric-lime accent. The proof card immediately after the metrics includes an authored attribution treatment with a character portrait beside the author details; it should not use a CSS-only abstract placeholder.

The portrait is a small circular photographic character image, positioned before the Figma attribution for **Marcus Vance, CMO at Nexus Bay**. The portrait can be cropped from the approved Figma reference only for this proof-card use.

The proof-card portrait is a compact circular crop that preserves the dark, editorial Case Study Detail color treatment. It is a supporting attribution detail rather than a separate testimonial module.
