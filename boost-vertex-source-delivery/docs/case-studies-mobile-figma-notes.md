# Case Studies Mobile Figma Notes

## Verified mid-page structure

The supplied mobile frame is a narrow single-column version of the Case Studies listing. After the compact hero and filter controls, the evidence cards remain vertically stacked. The Figma set visibly uses the card order CloudSync Tech, Lumina Retail, Vitalis Health Net, and a compact `Load More Case Studies` control. The lime `Performance Impact` block becomes a three-row vertical band: `$7M+` / `Ad Spend Managed`, `4.8x` / `Average ROI`, and `98%` / `Client Retention`.

The `Expertise Across High-Volume Industries` section follows, with the six industry cards arranged in a tight two-column grid. The visible labels include Fintech, SaaS, Healthcare, and Real Estate. The frame then leads into a centered quotation treatment on the dark background, followed by the conversion and footer regions lower in the page.

## Verified mobile hero and results entry

The mobile header is a compact obsidian strip with the lime `BOOST VERTEX` wordmark on the left and a small square hamburger trigger on the right. The hero reuses the dark olive/obsidian curve field, but crops it for a narrow vertical composition. Its centered display title is explicitly four lines: `Proven`, `Performance.`, `Quantifiable`, and `Growth.`; the first two are white and the final two use solid electric lime. A short centered supporting paragraph is followed by one full-width lime `Get My Free Growth Plan` action and a muted full-width `See Our Work` action.

The results region is a dark single-column control stack: Search Case Studies first, then a `Filter by:` label, followed by `All Industries` and `All Services` controls. The initial proof card is Nexus Capital Partners / Fintech / `+412% Organic Traffic`, followed by CloudSync Tech. Cards are full-width, dark, compactly padded, and separated vertically.

## Verified lower mobile composition

Below the stacked cards, the lime impact block remains three full-width consecutive rows. The industries section uses a vertical list of thin dark rows rather than a grid: Fintech, SaaS, E-commerce, Healthcare, Real Estate, and Logistics, each with a small lime icon. The quote is centered with a lime `99` marker, then a compact circular author image and the Marcus Vance / CMO, Nexus Capital Partners attribution.

The mobile conversion panel reads `Ready to Boost Your Business?`, with supporting copy, a full-width lime `Book a Strategy Call` button, and a dark outlined `View Our Services` button. The footer begins with the lime Boost Vertex name and brief positioning copy, then compact Services and Company link columns and its legal baseline.
